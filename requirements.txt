pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
pyarrow>=12.0
# Stage 1 of the detector only. Everything in overlay / sleeves / controls /
# benchmarks / validate works without these -- you just cannot call
# RegimeDetector.fit_predict().
jumpmodels>=0.1.1
xgboost>=2.0
